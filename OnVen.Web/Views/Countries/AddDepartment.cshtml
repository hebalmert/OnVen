@model OnVen.Common.Entities.Department

@{
    ViewData["Title"] = "Add Department";
}


<div class="row">
    <div class="col-md-6">
        <div class="shadow  bg-white rounded">
            <div class="card">
                <div class="card-header"><h4>New Department</h4></div>
                <div class="card-body">
                    <form asp-action="AddDepartment">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="CountryId" />

                        <partial name="_Department" />

                        <div class="form-group">
                            <a asp-action="Details" asp-route-id="@Model.CountryId" class="btn btn-outline-success btn-sm">
                                <i class="fa fa-backspace"></i>
                                Back
                            </a>

                            <button type="submit" value="Save" class="btn btn-outline-primary btn-sm">
                                <i class="fa fa-save"></i>
                                Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
